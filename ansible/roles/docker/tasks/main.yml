---
- name: Install Docker
  shell: curl -fsSL https://get.docker.com | sh

- name: Enable Docker
  service:
    name: docker
    enabled: yes
    state: started

- name: Add ubuntu to docker group
  user:
    name: ubuntu
    groups: docker
    append: yes
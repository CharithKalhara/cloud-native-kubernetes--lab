apiVersion: v1
kind: PersistentVolume

metadata:
  name: hello-app-pv

spec:
  capacity:
    storage: 1Gi

  accessModes:
    - ReadWriteOnce

  persistentVolumeReclaimPolicy: Retain

  hostPath:
    path: /data/hello-app
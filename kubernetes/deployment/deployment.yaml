apiVersion: apps/v1
kind: Deployment

metadata:
  name: hello-app
  namespace: production-lab
  labels:
    app: hello-app

spec:
  replicas: 1

  selector:
    matchLabels:
      app: hello-app

  template:
    metadata:
      labels:
        app: hello-app

    spec:
      containers:
        - name: hello-app
          image: YOUR_DOCKERHUB_USERNAME/hello-kubernetes:latest
          imagePullPolicy: Always

          ports:
            - containerPort: 3000

          env:
            - name: NAMESPACE
              value: production-lab

            - name: VERSION
              value: "1.0.0"

            - name: ENVIRONMENT
              value: Production

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"

            limits:
              cpu: "250m"
              memory: "256Mi"

          livenessProbe:
            httpGet:
              path: /api/info
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 15

          readinessProbe:
            httpGet:
              path: /api/info
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10